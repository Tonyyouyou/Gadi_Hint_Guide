interface:
  display_name: "Run on Gadi"
  short_description: "Safe Gadi interactive debug and batch workflows"
  default_prompt: "Use $run-on-gadi to classify this as static inspection, tmux interactive debug, or production batch, then apply the file-count-safe Gadi workflow."
